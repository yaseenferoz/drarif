import { Clock3, Mail, MapPin, Phone } from "@/components/icons";
import { site } from "@/lib/site-data";
export const metadata={title:"Contact"};
export default function Contact(){return <><section className="page-hero"><div className="shell"><span className="breadcrumb">Home / Contact</span><h1>We’re here to help.</h1><p>Contact the clinic for appointments, report review and specialist surgical consultation details.</p></div></section><section className="section"><div className="shell contact-grid"><div className="contact-cards"><ContactCard icon={<Phone/>} label="Appointment desk" value={site.phone} href={`tel:${site.phoneHref}`}/><ContactCard icon={<Mail/>} label="Email" value={site.email} href={`mailto:${site.email}`}/><ContactCard icon={<MapPin/>} label="Hospital" value={`${site.hospital}, ${site.location}`}/><ContactCard icon={<Clock3/>} label="Consultation hours" value={site.hours}/></div><iframe className="map" title="NK Hospital Kalaburagi" src="https://www.google.com/maps?q=NK%20Hospital%20Kalaburagi%20Karnataka&output=embed" loading="lazy"/></div></section></>}
function ContactCard({icon,label,value,href}:{icon:React.ReactNode,label:string,value:string,href?:string}){const body=<>{icon}<div><small>{label}</small><strong>{value}</strong></div></>;return href?<a className="contact-card" href={href}>{body}</a>:<div className="contact-card">{body}</div>}
